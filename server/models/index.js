const User = require("./User");
const Resource = require("./Resource");
const Events = require("./Events");
const Messages = require("./Instant-Messaging");
const Forum = require("./Forum");

module.exports = { User, Resource, Events, Messages, Forum };
